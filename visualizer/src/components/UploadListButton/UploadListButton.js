import React from 'react';

const ListItem = ({ name, id }) => {
    return (
        <div>
            <div>{key}</div>
            <div>{value}</div>
        </div>
    );
};

export default ListItem;
